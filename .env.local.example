# Base URL of the Flask backend (no trailing slash, no /api suffix - that's
# added by the API client). Set this in Vercel's project settings too
# (Project → Settings → Environment Variables) for Production/Preview.
NEXT_PUBLIC_API_URL=https://itp326png1checkpointcms-production.up.railway.app
